{{- $name := .name -}}
{{- $label := .label | default $name -}}
{{- $asset := resources.Get (printf "icons/%s.svg" $name) -}}

{{- if not $asset -}}
  {{- errorf "Cloudborg technology icon not found: icons/%s.svg" $name -}}
{{- end -}}

<img
  class="cb-tech-svg"
  src="{{ $asset.RelPermalink }}"
  alt="{{ $label }}"
  width="64"
  height="64"
  loading="eager"
  decoding="async">